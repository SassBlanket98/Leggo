// Leggo/.prettierrc.js
module.exports = {
  arrowParens: 'avoid', // Example: x => x
  bracketSameLine: false, // Puts > of multi-line JSX on a new line
  bracketSpacing: true, // Example: { foo: bar }
  singleQuote: true,    // Uses single quotes instead of double quotes
  trailingComma: 'all', // Trailing commas where valid in ES5 (objects, arrays, etc.)
  semi: true,           // Prints semicolons at the ends of statements
  printWidth: 100,      // Specifies the line length that the printer will wrap on
  tabWidth: 2,          // Specifies the number of spaces per indentation-level
  useTabs: false,       // Indent lines with spaces instead of tabs
};
